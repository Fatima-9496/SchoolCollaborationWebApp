@model IEnumerable<SchoolApp.Models.Announcement>

@{
    ViewData["Title"] = "School App";
    Layout = "~/Views/Shared/_TeacherLayout.cshtml";
}
<div class="offset-search">
    <form asp-controller="Teacher" asp-action="AnnouncementIndex">
        <input type="text" name="searchString" placeholder="Sarch here...">
        <button type="submit"><i class="fa fa-search"></i></button>
    </form>
</div>
<!-- offset search area end -->
<!-- body overlay area start -->
<div class="body_overlay"></div>
<!-- body overlay area end -->
<div class="course-area  pt--120 pb--70">
    <div class="container">
        @if (Model == null || !Model.Any())
        {
            <div class="col-md-10 offset-md-1">
                <div class="section-title-style2 black-title title-tb">
                    <a class="btn btn-primary btn-round btn-sm" asp-action="CreateAnnouncement">Create New Project</a>
                    <h2 class="primary-color">No Annoouncement Available</h2>
                </div>
            </div>
        }
        else
        {
        <div class="col-md-10 offset-md-1">
            <div class="section-title-style2 black-title title-tb">
                <a class="btn btn-primary btn-round btn-sm" asp-action="CreateAnnouncement">Create New Notice</a>
                <h2 class="primary-color">My Announcements</h2>
            </div>
        </div>
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-6">
                    <div class="card mb-5">
                        <img src="@item.AnnouncementPhoto" alt="image">
                        <div class="card-body  p-25">
                            <div class="course-meta-title mb-4">
                                <div class="course-meta-text">
                                    <h4><a href="course-details.html">@item.AnnouncementTitle</a></h4>
                                </div>
                            </div>
                            <p>@item.AnnouncementDescription</p>
                            <ul class="course-meta-details list-inline  w-100">
                                <li>
                                    <p>Post date</p>
                                    <span>@item.PostDate</span>
                                </li>
                                <li>
                                    <p>Author</p>
                                    <span>@item.AppUser.FirstName</span>
                                </li>
                                <li>
                                    <p>Document</p>
                                    <span><a href="~/@item.AnnouncementDocFile"><i class="fa fa-file" aria-hidden="true"></i></a></span>
                                </li>
                            </ul>
                            <br />
                            <a class="btn btn-primary btn-round btn-sm" asp-action="AnnouncementEdit" asp-route-id="@item.AnnouncementId">Edit</a>
                            <a class="btn btn-primary btn-round btn-sm" asp-action="AnnouncementDelete" asp-route-id="@item.AnnouncementId">delete</a>
                        </div><!-- card-body -->
                    </div><!-- card -->
                </div>
            }
        </div>
        }
    </div>
</div>

@*<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />

<p>
    <a asp-action="CreateAnnouncement">Create New Announcement</a>
</p>
<form asp-controller="Teacher" asp-action="AnnouncementIndex" method="get">
    <p>
        Title: <input type="text" name="SearchString" />
        <button type="submit"><i class="fas fa-search"></i></button>
        <a asp-action="AnnouncementIndex">Back to Full List</a>

    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AnnouncementTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnnouncementDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnnouncementPhoto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnnouncementDocFile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnnouncementAuthor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AnnouncementTitle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AnnouncementDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PostDate)
                </td>
                <td>
                    <img src="@item.AnnouncementPhoto" alt="User-Provided Image" />
                </td>
                <td>
                    <a href="~/@item.AnnouncementDocFile">View File</a>
                </td>                
                <td>
                    @Html.DisplayFor(modelItem => item.AnnouncementAuthor)
                </td>
                <td>
                    <a asp-action="AnnouncementEdit" asp-route-id="@item.AnnouncementId">Edit |</a>
                    <a asp-action="AnnouncementDelete" asp-route-id="@item.AnnouncementId">delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@