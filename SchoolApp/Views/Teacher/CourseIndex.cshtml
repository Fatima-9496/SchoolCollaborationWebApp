@model IEnumerable<SchoolApp.Models.Course>

@{
    ViewData["Title"] = "School App";
    Layout = "~/Views/Shared/_TeacherLayout.cshtml";
}

<div class="offset-search">
    <form asp-controller="Student" asp-action="CourseIndex">
        <input type="text" name="searchString" placeholder="Sarch here...">
        <button type="submit"><i class="fa fa-search"></i></button>
    </form>
</div>
<!-- offset search area end -->
<!-- body overlay area start -->
<div class="body_overlay"></div>
<!-- body overlay area end -->
<div class="course-area  pt--120 pb--70">
    <div class="container">
        <div class="col-md-10 offset-md-1">
            <div class="section-title-style2 black-title title-tb">
                <div class="section-title-style2 black-title title-tb">
                    <a class="btn btn-primary btn-round btn-sm" asp-action="CreateCourse">Create New Course</a>
                    <h2 class="primary-color">My Courses</h2>
                </div>            
            </div>
        </div>
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-6">
                    <div class="card mb-5">

                        <div class="card-body  p-25">
                            <div class="course-meta-title mb-4">
                                <div class="course-meta-text">
                                    <h4><a href="course-details.html">@item.CourseName</a></h4>
                                </div>
                            </div>
                            <p>@item.CourseDescription</p>
                            <ul class="course-meta-details list-inline  w-100">
                                <li>
                                    <p>Start Date</p>
                                    <span>@item.StartDate</span>
                                </li>
                                <li>
                                    <p>End Date</p>
                                    <span>@item.EndDate</span>
                                </li>
                                <li>
                                    <p>Document</p>
                                    <span><a href="@item.CourseDocFile"><i class="fa fa-file" aria-hidden="true"></i></a></span>
                                </li>
                            </ul>
                            <br />
                            <a class="btn btn-primary btn-round btn-sm" asp-action="CourseEdit" asp-route-id="@item.CourseId"> Edit </a>
                            <a class="btn btn-primary btn-round btn-sm" asp-action="CourseDelete" asp-route-id="@item.CourseId">delete</a>
                        </div><!-- card-body -->
                    </div><!-- card -->
                </div>
            }
        </div>
    </div>
</div>

@*<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
<p>
    <a asp-action="CreateCourse">Create New Course</a>
</p>
<form asp-controller="Teacher" asp-action="CourseIndex" method="get">
    <p>
        Title: <input type="text" name="SearchString" />
        <button type="submit"><i class="fas fa-search"></i></button>
        <a asp-action="CourseIndex">Back to Full List</a>
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseMaterial)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @if (@item.CourseDocFile != null)
                    {
                        <a href="~/@item.CourseDocFile">View File</a>
                    }
                </td>
                <td>
                    <a asp-action="CourseEdit" asp-route-id="@item.CourseId">Edit |</a>
                    <a asp-action="CourseDelete" asp-route-id="@item.CourseId">delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@